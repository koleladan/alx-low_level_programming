#include "main.h"

#include <string.h>



/**
 * _strlen - functuon to find the length of a string
 * @s: is an ascii character.
 * Return: length of string
 */

int _strlen(char *s)

{

	int a;



	a = strlen(s);



	return (a);

i}
