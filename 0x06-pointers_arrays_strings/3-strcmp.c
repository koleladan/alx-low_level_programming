#include "main.h"

#include <string.h>



/**
 * _strcmp - check if two strings are the same
 * @s1: first string
 * @s2: 2nd string
 * Return: less than 0 if , 0 if the same and > 0 if
 */

int _strcmp(char *s1, char *s2)

{

	int a;



	a = strcmp(s1, s2);



	return (a);

}
