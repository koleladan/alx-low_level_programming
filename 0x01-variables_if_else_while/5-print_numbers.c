#include <stdio.h>

/** 
 * main - printing digits from 0 to 10
 *
 * Return: Always o(success)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		printf("%d", num);

	printf("\n");

	return (0);
}
